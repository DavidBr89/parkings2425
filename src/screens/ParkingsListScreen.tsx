import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ParkingsListScreen = () => {
  return (
    <View>
      <Text>ParkingsListScreen</Text>
    </View>
  );
};

export default ParkingsListScreen;

const styles = StyleSheet.create({});
